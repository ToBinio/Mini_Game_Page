{
  "name": "client",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "dev": "nodemon --ignore dist --ext html,ts,css --exec npm run build",
    "build": "tsc && vite build"
  },
  "devDependencies": {
    "@types/js-cookie": "^3.0.2",
    "@types/socket.io-client": "^3.0.0",
    "nodemon": "^2.0.16",
    "typescript": "^4.5.4",
    "vite": "^2.9.9"
  },
  "dependencies": {
    "js-cookie": "^3.0.1",
    "socket.io-client": "^4.5.1"
  }
}
