{
  "name": "client",
  "version": "1.0.0",
  "description": "",
  "main": "src/index.js",
  "scripts": {
    "build": "snowpack build",
    "dev": "nodemon --ignore build --ext html,js,css --exec npm run build"
  },
  "dependencies": {
    "socket.io-client": "^4.5.1"
  },
  "devDependencies": {
    "@types/socket.io-client": "^3.0.0",
    "nodemon": "^2.0.16",
    "snowpack": "^3.8.8",
    "typescript": "^4.6.4"
  }
}
